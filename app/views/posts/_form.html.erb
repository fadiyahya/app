<%= form_for(@post) do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @post.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "Post Title" %><br />
    <%= f.text_field :title %>
  </div>
  
    <div class="field">
    <%= f.label "Category" %><br />
    <%= f.radio_button :tag_list, "Events" %> Events
    <%= f.radio_button :tag_list, "Forsale" %> For Sale
    <%= f.radio_button :tag_list, "Jobs" %> Jobs
    <%= f.radio_button :tag_list, "Housing" %> Housing
    <%= f.radio_button :tag_list, "Deals" %> Deals
  </div>
  

  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
  
  <div class="field">
    <%= f.label :lifespan %><br />
    <%= f.select("lifespan", [5,15,25]) %>
  </div>

  <div class="field">
    <!--<%#= f.collection_select(:user_id, User.all, :id, :email) %>-->
  <div>

  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
